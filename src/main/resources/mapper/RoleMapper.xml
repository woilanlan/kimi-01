<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="top.hxll.kimi.mapper.RoleMapper">

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultType="top.hxll.kimi.entity.Role">
        SELECT r.*
        FROM sys_role r
        JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.deleted = 0 AND r.status = 1
        ORDER BY r.sort_order ASC, r.id ASC
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultType="top.hxll.kimi.entity.Role">
        SELECT *
        FROM sys_role
        WHERE role_code = #{roleCode} AND deleted = 0 AND status = 1
        LIMIT 1
    </select>

</mapper>